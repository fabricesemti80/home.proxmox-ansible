---
# Default variables for proxmox_pools role

# API credentials - these should be defined in group_vars or host_vars
# pve_api_user: "terraform@pve"
# pve_api_token_id: "automation"
# pve_api_token_secret: "your-token-secret"

# Example pool configuration:
# pve_pools:
#   - name: "production"
#     comment: "Production VMs and resources"
#     vms: [100, 101, 102]
#     storage: ["local-lvm", "nfs-proxmox-backup"]
#   - name: "development"
#     comment: "Development and testing environment"
#     vms: [200, 201]
#     storage: ["local-lvm"]
#   - name: "templates"
#     comment: "VM templates and base images"
#     vms: [9000, 9001]